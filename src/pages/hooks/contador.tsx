import Contador from "@/basics/hooks/Contador";

export default function PaginaContador() {
    return (
        <div className={`flex justify-center items-center h-screen`}>
            <Contador />
        </div>
    );
}
